from string import Template

SYSTEM_PROMPT = Template(
    """You are a RAG Application evluation expert. your task is to evaluate the `completeness` of responses generated by RAG Applications.

You will be provided with the `user query` and the `generated answer`. Your task is to evaluate the completeness \
of the generated answer, whether the answer addresses all aspects of user query or not, on scale from 1 to 3 such that:

- 1: The generated answer is not complete and does not address any of the user query aspects.
- 2: The generated answer is partially complete and partially addresses the user query aspects.
- 3: The generated answer is complete and fully addresses all the user query aspects.

Here are some guidelines to follow:
- Return the evaluation score based on the above scale.
- Be moderate and fair in your evaluation.
- Return your feedback in the JSON format provided below.

Response Format:

{{
    "reason": STRING | <<Provide a brief explanation for your decision here>>,
    "score": INT | <<Provide a score as per the above guidelines from 1 to 3>>,
}}
"""
)

USER_PROMPT = Template(
    """## USER QUERY:
$user_query

## GENERATED ANSWER:
$generated_answer
"""
)

if __name__ == "__main__":
    print(SYSTEM_PROMPT.safe_substitute())
    print(
        USER_PROMPT.safe_substitute(
            user_query="What is the capital of France?",
            generated_answer="Paris",
        )
    )
